0. Запишете си присъствията с парола: sjzidh
             създаване на MESH: 
let geometry = new Three.NeshtoSiGeometry()
let material = new Three.NeshtoSiMaterial()
let mesh = new Three.Mesh(geometry, material);
scene.add(mesh)
            Промени на Mesh:
- мърдане:          * mesh.position.set(x,y,z);     * mesh.position.x = ???
- ротация:          * mesh.rotation.set(x, y, z);   * mesh.rotation.z = ???
- скалиране(размер):* mesh.scale.set(x, y, z)       * mesh.scale.y = ???
            камерата:
camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000 );
*   camera.position
*   camera.up.set(0, 0, 1)
*   camera.lookAt(x, y, z); завърта камерата, така че тя да гледа към x,y,z
1. Махнете автоматичното въртене на камерата
2. При натискане на 1,2,3,4,5 нека сменяме:
* позицията на камерата
* накъде гледа камерата
Изберете си няколко яки позиции на камерата.
3. Играч, мърда се напред/ назад с задържане на W/S (вижте window.addEventListener("keydown"))
* играчът е каквато геометрия искате (забранявам паралелепипед, сфера, цил.)
4. Със задържане на A/D променяме ротацията на играча
5. Играчът не се мърда само напред назад. 
Той винаги мърда в посоката, в която е врътнат


Домашна работа:
1. Довършете движението на играча:
- W/S мърдаме напред назад
- A/D завиваме

2. Направете така, че движението на играча да е плавно
(сега играчът се мръдва, чака и после започва движението)

3. Направете first person DOOM камера
- камерата винаги се намира на позицията на играча
- камерата винаги гледа към следващата позиция на играча (там къде ще бъдем като натиснем W)
- camera.up.set(0,0,1)


